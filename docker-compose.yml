version: "3.9"

services:
  db:
    image: mysql:8.0
    container_name: requestweb-mysql
    environment:
      MYSQL_ROOT_PASSWORD: "@Ori2305"
      MYSQL_DATABASE: web
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./web.sql:/docker-entrypoint-initdb.d/1-init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p@Ori2305"]
      interval: 5s
      timeout: 3s
      retries: 20

  web:
    build: .
    container_name: requestweb-app
    depends_on:
      db:
        condition: service_healthy
    working_dir: /app
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: "@Ori2305"
      DB_NAME: web
      DB_PORT: 3306
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  db_data:

